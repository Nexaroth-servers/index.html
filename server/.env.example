# Example environment variables for Nexaroth backend

# JWT secret for signing tokens (change in production)
JWT_SECRET=change_me_jwt_secret

# Webhook secret for vote/donation webhooks (change in production)
WEBHOOK_SECRET=change_me_webhook_secret

# Path to the SQLite database file (optional)
DB_PATH=./server/nexaroth.db

# Port
PORT=3000
